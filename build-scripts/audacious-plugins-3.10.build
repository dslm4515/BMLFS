#! /bin/bash

# source: https://distfiles.audacious-media-player.org/audacious-plugins-3.10.tar.bz2

./configure --prefix=/usr --build=x86_64-linux-musl --enable-qt &&

read -p "Compile? " && make -j4 &&
read -p "Install? " && sudo -S porg -lD "make -j1 install"
