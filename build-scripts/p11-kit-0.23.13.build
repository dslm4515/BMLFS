#! /bin/bash

./configure --prefix=/usr     \
	    --sysconfdir=/etc \
            --with-trust-paths=/etc/pki/anchors $BUILD &&
read -p "Compile?" && make -j2 && 
read -p "Install?" && sudo -S porg -lD "make install -j1 "
