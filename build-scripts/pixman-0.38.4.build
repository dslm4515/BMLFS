#! /bin/bash

# Pixman 0.38.4
# Source: https://www.cairographics.org/releases/pixman-0.38.4.tar.gz

./configure --prefix=/usr \
            --disable-static $BUILDTRUPLE &&
read -p "Compile? " && make -j2 && 
read -p "Install? " && sudo -S porg -lD "make install"
