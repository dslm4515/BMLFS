#! /bin/bash

# source: http://files.libburnia-project.org/releases/libburn-1.4.8.tar.gz

./configure --prefix=/usr $BUILD --disable-static &&

read -p "Compile? " && make -j2 &&
read -p "Install? " && sudo -S porg -lD "make -j1 install"

