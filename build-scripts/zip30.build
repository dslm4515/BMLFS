#! /bin/bash

# Zip 3.0
# source: https://downloads.sourceforge.net/infozip/zip30.tar.gz

patch -Np1 -i ../patches/10-zip-3.0-build.patch &&
patch -Np1 -i ../patches/20-zip-3.0-exec-stack.patch  &&


make -f unix/Makefile generic_gcc -j2 &&
sudo -S porg -lp zip-3.0 "make prefix=/usr MANDIR=/usr/share/man/man1  -f unix/Makefile install"
