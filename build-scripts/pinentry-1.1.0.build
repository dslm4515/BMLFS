#! /bin/bash

./configure --prefix=/usr --enable-pinentry-tty \
            --enable-pinentry-gtk2=yes \
            --enable-pinentry-gnome3=yes $BUILD   &&
read -p "Compile? " && make -j2 && 
read -p "Install? " && sudo -S porg -lD "make install -j1" 
