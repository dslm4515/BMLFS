#! /bin/bash

mkdir build && cd build &&
cmake -DCMAKE_INSTALL_PREFIX=/usr \
      -DBUILD_SHARED_LIBS=1 .. &&
read -p "Compile? " && make -j2  && 
read -p "Install? " && sudo -S porg -lp snappy-1.1.7 "make install -j1"
