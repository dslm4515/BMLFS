#! /bin/bash

# source: https://downloads.sourceforge.net/lcms/lcms2-2.9.tar.gz

./configure --prefix=/usr --disable-static  --build=x86_64-linux-musl &&

read -p "Compile? " && make -j4 &&
read -p "Install? " && sudo -S porg -lD "make -j1 install"

