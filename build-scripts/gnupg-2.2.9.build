#! /bin/bash

./configure --prefix=/usr  \
            --enable-symcryptrun     \
            --docdir=/usr/share/doc/gnupg-2.2.9  $BUILD &&
read -p "Compile? " && make -j2 && 
read -p "Install? " && sudo -S porg -lD "make install -j1" 
