#! /bin/bash

sed -i -e '/cat./d' documentation/Makefile &&

./configure --prefix=/usr $BUILD $HOST $TARGET --enable-shared &&

read -p "Compile? " && make -j2 &&
read -p "Install? " && sudo -S porg -lD "make -j1 docdir=/usr/share/doc/fltk-1.3.4 install"
