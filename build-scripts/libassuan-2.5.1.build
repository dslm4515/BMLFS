#! /bin/bash

# source: https://www.gnupg.org/ftp/gcrypt/libassuan/libassuan-2.5.1.tar.bz2

./configure --prefix=/usr $BUILD &&
read -p "Compile? " && make -j2 && 
read -p "Install? " && sudo -S porg -lD "make install -j1"
