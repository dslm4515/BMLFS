#! /bin/bash

# PAVUcontrol 4.0 
# source: http://freedesktop.org/software/pulseaudio/pavucontrol/pavucontrol-4.0.tar.gz

./bootstrap.sh --prefix=/usr $BUILDTRUPLE &&

read -p "Compile? " && make -j2 &&
read -p "Install? " && sudo -S porg -lD "make install"
