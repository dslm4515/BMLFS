#! /bin/bash

# Tiff 4.0.10
# Source: http://download.osgeo.org/libtiff/tiff-4.0.10.tar.gz

autoreconf -vfi &&
./configure --prefix=/usr $BUILD  \
            --docdir=/usr/share/doc/libtiff-4.0.10 &&
read -p "Compile? " && make -j2 && 
read -p "Install? " && sudo -S porg -lD "make install" 
