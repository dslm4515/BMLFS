#! /bin/bash

# libxkbcommon-0.8.4 
# source: https://xkbcommon.org/download/libxkbcommon-0.8.4.tar.xz

./configure $XORG_CONFIG \
            --docdir=/usr/share/doc/libxkbcommon-0.8.2 &&
read -p "Compile? " && make -j2 && 
read -p "Install? " && sudo -S porg -lD "make install"
