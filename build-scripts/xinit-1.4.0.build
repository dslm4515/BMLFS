#! /bin/bash

sed -e '/$serverargs $vtarg/ s/serverargs/: #&/' \
	    -i startx.cpp &&
./configure $XORG_CONFIG $BUILD --with-xinitdir=/etc/X11/app-defaults &&
read -p "Compile? " && make -j2 && 
read -p "Install? " && sudo -S porg -lD "make install -j1" 
