#! /bin/bash

# source: http://ftp.gnome.org/pub/gnome/sources/libpeas/1.22/libpeas-1.22.0.tar.xz

enable_glade_catalog=yes \
./configure --prefix=/usr $BUILD &&
read -p "Compile? " && make -j2 &&
read -p "Install? " && sudo -S porg -lD "make -j1 install"
