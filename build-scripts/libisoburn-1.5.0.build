#! /bin/bash

# libisoburn 1.5.0
# source: http://files.libburnia-project.org/releases/libisoburn-1.5.0.tar.gz

./configure --prefix=/usr $BUILDTRUPLE \
     --disable-static --enable-pkg-check-modules &&

read -p "Compile? " && make -j2 &&
read -p "Install? " && sudo -S porg -lD "make install"
