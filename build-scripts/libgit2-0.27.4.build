#! /bin/bash
mkdir -v build && cd build &&
CFLAGS="-D__ANDROID_API__" \
cmake -DCMAKE_INSTALL_PREFIX=/usr \
      -DTHREADSAFE=ON .. &&
read -p "Compile? " && make -j2  && 
read -p "Install? " && sudo -S porg -lp libgit2-0.27.4 "make install -j1"
