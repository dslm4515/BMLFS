#! /bin/bash

# source: https://github.com/downloads/nicklan/pnmixer/pnmixer-0.5.1.tar.gz

./autogen.sh --prefix=/usr $BUILD &&

read -p "Compile? " && make -j2 &&
read -p "Install? " && sudo -S porg -lD "make -j1 install"
