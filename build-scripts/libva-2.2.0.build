#! /bin/bash

# source: https://github.com/intel/libva/releases/download/2.2.0/libva-2.2.0.tar.bz2

./configure $XORG_CONFIG $BUILD --disable-glx  &&
read -p "Compile? " && make -j2 && 
read -p "Install? " && sudo -S porg -lD "make install -j1" 
