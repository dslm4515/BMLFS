#! /bin/bash

# source: http://git.savannah.nongnu.org/cgit/xbindkeys.git/snapshot/xbindkeys-1.8.6.tar.gz

./configure --prefix=/usr $BUILD &&

read -p "Compile? " && make -j2 &&
read -p "Install? " && sudo -S porg -lD "make -j1 install"
