#! /bin/bash

# source:  https://dbus.freedesktop.org/releases/dbus-python/dbus-python-1.2.8.tar.gz

mkdir python2 &&
pushd python2 &&
PYTHON=/usr/bin/python     \
../configure $BUILD --prefix=/usr --docdir=/usr/share/doc/dbus-python-1.2.8 &&
make -j2 &&
popd &&

read -p "Build forpython3?" &&

mkdir python3 &&
pushd python3 &&
PYTHON=/usr/bin/python3    \
../configure $BUILD --prefix=/usr --docdir=/usr/share/doc/dbus-python-1.2.8 &&
make -j2 &&
popd &&

read -p "Install? " && 
sudo -S -E porg -lD  "make -C python2 install" &&
sudo -S -E porg -lD+ "make -C python3 install"

