#! /bin/bash

# source: https://ftp.gnu.org/gnu/wget/wget-1.19.5.tar.gz

./configure --prefix=/usr      \
            --sysconfdir=/etc  \
            --with-ssl=openssl $BUILD &&
read -p "Compile? " && make -j2 && 
read -p "Install? " && sudo -S porg -lD "make install -j1"
