#! /bin/bash

# Wayland Protocols 1.18
# Source: https://wayland.freedesktop.org/releases/wayland-protocols-1.18.tar.xz

./configure --prefix=/usr $BUILDTRUPLE  &&
read -p "Compile? " && make -j2 && 
read -p "Install? " && sudo -S porg -lD "make install"
