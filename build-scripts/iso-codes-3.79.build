#! /bin/bash

# source : http://anduin.linuxfromscratch.org/BLFS/iso-codes/iso-codes-3.79.tar.xz

./configure --prefix=/usr --build=x86_64-linux-musl &&

read -p "Compile? " && make -j4 &&
read -p "Install? " && sudo -S porg -lD "make -j1 install"

