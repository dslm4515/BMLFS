#! /bin/bash

./configure --prefix=/usr     \
            --disable-static  \
            --enable-lib-only \
            --docdir=/usr/share/doc/nghttp2-1.32.0 --build=x86_64-linux-musl 
read -p "Compile?" && make -j4  && 
read -p "Install?" && sudo -S porg -lD "make install -j1 "
